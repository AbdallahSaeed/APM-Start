import { ProductHttpSercice } from './product-http-sercice';

describe('ProductHttpSercice', () => {
  it('should create an instance', () => {
    expect(new ProductHttpSercice()).toBeTruthy();
  });
});
